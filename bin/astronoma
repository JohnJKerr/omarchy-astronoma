#!/usr/bin/env python3
"""Astronoma CLI entry point — the plugin's only way into the machine."""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.realpath(__file__)), "..", "helper"))

from astronoma.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
